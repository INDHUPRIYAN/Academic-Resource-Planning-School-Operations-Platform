# Login assets

Drop the classroom hero image here as **login-hero.jpg** (or .png and update the
reference in index.html). The login page references `assets/login-hero.jpg` for the
left panel and falls back to a branded gradient if the file is absent.
